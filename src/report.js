import { REFERENCES } from './parameters.js';
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
export function jsonText(est){return JSON.stringify(est,null,2);}
export function csvText(est){const q=s=>'"'+String(s??'').replaceAll('"','""')+'"';const rows=[['turn','input_tokens','output_tokens','text_energy_Wh','tool_energy_Wh','total_energy_Wh','water_mL','user_prompt']];for(const t of est.turns)rows.push([t.turn,t.input_tokens,t.output_tokens,t.text_energy_wh.toFixed(6),t.tool_energy_wh.toFixed(6),t.total_energy_wh.toFixed(6),t.total_water_ml.toFixed(6),t.user_excerpt]);return rows.map(r=>r.map(q).join(',')).join('\n');}
export function htmlText(est){const rows=est.turns.map(t=>`<tr><td>${t.turn}</td><td>${t.input_tokens}</td><td>${t.output_tokens}</td><td>${t.total_energy_wh.toFixed(3)}</td><td>${t.total_water_ml.toFixed(3)}</td></tr>`).join('');const refs=REFERENCES.map(([n,u])=>`<li><a href="${esc(u)}">${esc(n)}</a></li>`).join('');return `<!doctype html><meta charset="utf-8"><title>Prompt2Water report</title><style>body{font-family:system-ui;max-width:960px;margin:40px auto;padding:0 20px;color:#16202a}table{border-collapse:collapse;width:100%}th,td{padding:8px;border-bottom:1px solid #ddd;text-align:right}th:first-child,td:first-child{text-align:left}.big{font-size:2rem;font-weight:700}.muted{color:#667}</style><h1>Prompt2Water audit</h1><p class=muted>${esc(est.provider)} · ${esc(est.title)} · ${esc(est.model)} · ${esc(est.thinking)}</p><div class=big>${est.total_water_ml.toFixed(2)} mL</div><p>Scenario range: ${est.scenario_low_ml.toFixed(2)}–${est.scenario_high_ml.toFixed(2)} mL</p><p>Estimated energy: ${est.total_energy_wh.toFixed(3)} Wh · direct water: ${est.direct_water_ml.toFixed(2)} mL · electricity-related water: ${est.electricity_water_ml.toFixed(2)} mL</p><h2>Turn-by-turn</h2><table><tr><th>Turn</th><th>Input tokens</th><th>Output tokens</th><th>Energy (Wh)</th><th>Water (mL)</th></tr>${rows}</table><h2>Assumptions</h2><ul>${est.assumptions.map(x=>'<li>'+esc(x)+'</li>').join('')}</ul><h2>References</h2><ol>${refs}</ol>`;}
export function downloadText(filename,text,type){const blob=new Blob([text],{type});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=filename;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1000);}
